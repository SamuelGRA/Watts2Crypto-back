services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      APP_CORS_ALLOWED_LOCAL_HOSTS: localhost,127.0.0.1,::1
    volumes:
      - backend-data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ../Watts2Crypto-front
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "3000:8080"
    restart: unless-stopped

volumes:
  backend-data: